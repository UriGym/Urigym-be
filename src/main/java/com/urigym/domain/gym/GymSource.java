package com.urigym.domain.gym;

public enum GymSource {
    /** Registered by an owner through the app. */
    USER,
    /** Imported from Kakao Local search, unclaimed until an owner registers. */
    KAKAO
}
