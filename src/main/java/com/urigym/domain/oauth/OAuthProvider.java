package com.urigym.domain.oauth;

public enum OAuthProvider {
    KAKAO,
    NAVER
    // APPLE: deferred — needs a paid Apple Developer Program membership and an
    // ES256-signed client secret. Add it here plus an AppleOAuthClient when ready.
}
